<% @posts.each do |post|%>
  <div class="post container">
    <h2><%= post.title_cn %></h2>
    <div class="body">
      <%= GitHub::Markdown.render_gfm(post.body).html_safe %>
    </div>
  </div>
<% end %>
