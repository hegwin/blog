<%= form_for([:admin, @post], html: { class: "form-horizontal"}) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= f.label :title_cn, class: "form-label" %>
    <%= f.text_field :title_cn, class: "form-control" %>
  </div>

  <div class="mb-3">
    <%= f.label :title_en, class: "form-label" %>
    <%= f.text_field :title_en, class: "form-control" %>
  </div>

  <div class="mb-3">
    <%= f.label :tag_list, class: "form-label" %>
    <%= f.text_field :tag_list, class: "form-control", value: @post.tag_list.join(', ') %>
  </div>

  <div class="mb-3">
    <%= f.label :body, class: "form-label" %>
    <%= f.text_area :body, class: "form-control", rows: 20 %>
  </div>

  <div class="mb-3">
    <%= f.label :posted_on, class: "form-label" %>
    <%= f.date_select :posted_on, class: "form-select" %>
  </div>

  <div class="form-actions actions">
    <%= f.submit class: "btn btn-primary" %>
  </div>
<% end %>
